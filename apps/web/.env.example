# Supabase Postgres (transaction pooler). Used by `next dev`; Workers use Hyperdrive in production.
DATABASE_URL="postgresql://postgres.tbexpdwipdjgcjtlujis:[password]@aws-1-us-east-2.pooler.supabase.com:6543/postgres"

# Public site URL (metadata, OG, share links). Production: https://hunnybibletracker.com
NEXT_PUBLIC_SITE_URL="http://127.0.0.1:3000"

# Supabase Auth — server-side JWT verification and admin API access.
SUPABASE_URL="https://tbexpdwipdjgcjtlujis.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Supabase Auth — browser admin dashboard only (never use service role here).
NEXT_PUBLIC_SUPABASE_URL="https://tbexpdwipdjgcjtlujis.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Admin emails allowed to use the web admin dashboard.
ADMIN_EMAILS="admin@example.com"

# Cloudinary configuration for plan cover image uploads.
CLOUDINARY_CLOUD_NAME="your-cloud-name"
# Same value — required for Today message share preview in the admin UI (client component).
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
