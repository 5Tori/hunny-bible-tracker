# Hunny Bible Tracker API

Placeholder Next.js API app for future Neon backup/sync.

The v0.1 mobile app is local-first and does not require this API yet.

Planned endpoints:

```text
GET  /api/health
POST /api/sync/push
GET  /api/sync/pull
GET  /api/sync/status
POST /api/backup/export
POST /api/backup/restore
```
